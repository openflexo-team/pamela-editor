package test.model3;

import org.openflexo.pamela.AccessibleProxyObject;
import org.openflexo.pamela.annotations.ModelEntity;

/** No declared property → entity style UNDETERMINED (effective style = the preference default). */
@ModelEntity
public interface EmptyEntity extends AccessibleProxyObject {
}
