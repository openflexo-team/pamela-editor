package test.model1;

/**
 * A plain implementation class, not yet attached to {@link Foo1} via
 * {@code @ImplementationClass} — used to exercise the "attach existing class" mutation
 * (implementation-class-support-design.md).
 */
public abstract class Foo1Impl implements Foo1 {

}
